name: Deploy Frontend
on : 
  push:
    brances:
      - master

jobs:
   build:
     runs-on: ubuntu-latest
     steps:
        - uses: actions/checkout@v2 
        - run : echo "Hello actions"
  

        
 